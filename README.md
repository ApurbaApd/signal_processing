# Neural signal analysis

This repo contains Python pipelines for analysing LFP-like neural data and runs basic analyses: filtering, PSD (Welch), spectrogram, band-limited burst detection, and event-locked averaging (ERP).
